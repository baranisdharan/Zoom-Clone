import { Module } from '@nestjs/common';
// Future: Import authentication services

/**
 * Auth Module - Skeleton for future authentication implementation
 * 
 * Future features:
 * - JWT authentication
 * - Local strategy
 * - OAuth providers (Google, Microsoft)
 * - Guards for routes and WebSocket
 */
@Module({
    imports: [],
    providers: [],
    exports: [],
})
export class AuthModule { }
